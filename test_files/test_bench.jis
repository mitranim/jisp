;;
This file should be used for benching.

This assumes that the prelude is pre-imported into the module scope in mixin
mode. We do that for this benchmark because `use.mac` is always asynchonous,
but we want to run the benchmark synchronously.
;;

123
123.456
`some_string`
"some_string"

````
some
multiline
string
````

""""
some
multiline
string
""""

const pubConst
  func
    privFunc one two three
    if one two three
    if one.two two.three three.four

func
  pubFunc one two three
  if one
    do two three
    do one two three
  if one
    do two.three three.four
    do one two three
  if one
    do two.three.four three.four.five
    do one two three

class
  PubCls pubFunc
  let someField `some_value`

  func
    someMethod one two three
    if one
      do two three
      do one two three
    if one
      do two.three three.four
      do one two three
    if one
      do two.three.four three.four.five
      do one two three

  do
    if 10 20 30
    if 10 20 30
    if 10 20 30
