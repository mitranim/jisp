(angular.module "jispApp.controller" `())

(.controller "MainControl" (ngFn $scope $location $anchorScroll (do
  (= $scope.gotoTop
  ; doesn't clear native anchor hash, todo fix
    {do ($location.hash "top")
        ($anchorScroll)
        ($location.hash "")})
  (console.log (jisp.eval '(prn "If we’re seeing this, jisp.eval works.")'))
))) ; / MainControl
